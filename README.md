# gitscope
A terminal UI (TUI) application for visualizing git commit history — log view, branch graph, and a diff pane — all read-only (no mutating git operations, no staging/committing/rebasing from the tool).
